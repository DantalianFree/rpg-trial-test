#pragma once
#include "Character.h"
#include <iostream>
#include <string>

class Create {
public:
    static Character characterCreation( int cType ) {
        switch( cType ) {
            case 1:
                
        }
    }

}